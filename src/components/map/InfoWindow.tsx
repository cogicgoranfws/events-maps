import React, { useEffect, useState } from 'react';

interface Props {
    
}

function InfoWindow(props: Props): null {
    const [info, setInfo] = useState<any>();

    

    return null
};

export default InfoWindow;